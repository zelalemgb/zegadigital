'use strict';

/**
 * Afaan Oromo content pack — PLACEHOLDER / TODO.
 *
 * Same rules as the Amharic pack (../am/index.js): this object is deep-merged
 * over English, so the bot works in "Afaan Oromo" today (showing English) and
 * you translate incrementally. Mirror the shape of ../en/*.js and keep every
 * `input` / `next` / `answer` key identical to English — translate only text.
 */

module.exports = {
  meta: { code: 'om', name: 'Afaan Oromo', complete: false },
  // strings: {},
  // nodes: {},
  // glossary: {},
  // quizzes: {},
};
